# NHL Point Prediction API - local run
# Ensure models/ and data/features/ exist (run pipeline first)
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
